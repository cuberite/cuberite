version: 1.0.{build}
configuration: Debug
clone_depth: 1
before_build:
- echo %TIME%
- git submodule update --init
- echo %TIME%
- cmake .
- echo %TIME%
build:
  project: Cuberite.sln
  parallel: true
  verbosity: minimal
