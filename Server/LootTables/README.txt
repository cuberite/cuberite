This folder contains the default global loot tables.
Upon replacing/modifying them you will change gameplay behaviour.

When placing a valid loot table file in a folder called "User" it's accesible with the string "User|%Filename%"